{
  "name": "microservices",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "docker-compose:up": "docker-compose --file=docker-compose.dev.yml up -d --build --remove-orphans",
    "docker-compose:stop": "docker-compose --file=docker-compose.dev.yml stop"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
